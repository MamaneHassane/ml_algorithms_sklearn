# Va faire plusieurs arbres de décisions différents
# Tous les arbres n'utilisent pas tout le dataset, mais ils utilisent des sous ensembles
# C'est "l'ensemble learning"
# C'est une "parallel learning"

# Le modèle est l'ensemble des arbres
# La prédiction est la moyenne de toutes les prédictions